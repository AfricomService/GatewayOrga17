# ===================================================================
# Spring Cloud Config bootstrap configuration for the "prod" profile
# ===================================================================

spring:
  cloud:
    config:
      fail-fast: true
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 100
      uri: http://africomServices:${jhipster.registry.password}@${hostserver}:8761/config
      # name of the config server's property source (file.yml) that we want to use
      name: orgacaregateway
      profile: prod # profile(s) of the property source
      label: master # toggle to switch to a different version of the configuration as stored in git
      # it can be set to any label, branch or commit of the configuration source Git repository

jhipster:
  registry:
    password: africomOrgaCareRegistryEureka

# ── Serveur ───────────────────────────────────────────
hostserver: 172.30.1.111
hostserverKeyCloak: 172.30.1.111

# ── OAuth2 / OIDC ─────────────────────────────────────
OAUTH2_ISSUER_URI: http://${hostserverKeyCloak}:9080/realms/Africom
OAUTH2_CLIENT_ID: web_app
OAUTH2_CLIENT_SECRET: web_app
OAUTH2_SCOPE: openid,profile,email

# ── ApplicationProperties du Gateway ──────────────────
KEYCLOAK_ADMIN_USER_CREATE_URL: http://${hostserverKeyCloak}:9080/admin/Africom/console/#/Africom/users/add-user
KEYCLOAK_SERVER_URL: http://${hostserverKeyCloak}:9080
KEYCLOAK_ADMIN_USERNAME: admin
KEYCLOAK_ADMIN_PASSWORD: admin
KEYCLOAK_TARGET_REALM: Africom
